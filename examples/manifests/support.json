{
  "agentId": "support",
  "compaction": {
    "enabled": true,
    "maxOverflowRetries": 1,
    "retainBudget": 0.16,
    "summarizationMaxTokens": 8192,
    "summarizationModel": null,
    "thresholdRatio": 0.8
  },
  "instructionsPath": "agents/support/instructions.md",
  "instructionsSha256": "bcb2e4dfd83cdddc52fae602d0119dccf6ab8f40f67428236c528166877dbdff",
  "limits": {
    "delegationMaxConcurrent": 1,
    "delegationMaxPerTurn": 2,
    "maxResultBytes": null,
    "maxTurns": 12
  },
  "model": {
    "contextWindow": null,
    "maxTokens": 1024,
    "name": "claude-3-5-haiku-20241022",
    "provider": "anthropic"
  },
  "repeatGuard": {
    "enabled": true,
    "exclude": [
      "ask_user",
      "load_skill"
    ],
    "previewCap": 500,
    "thresholds": [
      3,
      5,
      8
    ]
  },
  "skills": [
    {
      "description": "How to evaluate refund eligibility and process a refund request for a customer order.",
      "id": "refund-policy",
      "name": "Refund Policy",
      "path": "agents/support/skills/refund-policy",
      "source": "authored"
    }
  ],
  "subagentIds": [
    "researcher"
  ],
  "tools": [
    {
      "approval": "never",
      "description": "Ask the human user a question when you need information that only they can provide (a decision, a missing fact, a preference). Calling this tool pauses the run until the human answers; the answer is returned as this call's result on the next turn.",
      "executable": false,
      "idempotent": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "options": {
            "description": "Optional suggested answers to offer the human user.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "question": {
            "description": "The question to put to the human user.",
            "type": "string"
          }
        },
        "required": [
          "question"
        ],
        "type": "object"
      },
      "name": "ask_user",
      "source": "builtin"
    },
    {
      "approval": "once",
      "description": "Flag a customer account for manual review by the trust & safety team.",
      "executable": true,
      "idempotent": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "customer_id": {
            "description": "The customer id to flag for review.",
            "title": "Customer Id",
            "type": "string"
          },
          "reason": {
            "description": "Why this account is being flagged.",
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "customer_id",
          "reason"
        ],
        "type": "object"
      },
      "name": "flag_account",
      "source": "bundle:crm"
    },
    {
      "approval": "never",
      "description": "Load the full instructions for one of this agent's available skills by id.",
      "executable": true,
      "idempotent": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "skill": {
            "enum": [
              "refund-policy"
            ],
            "type": "string"
          }
        },
        "required": [
          "skill"
        ],
        "type": "object"
      },
      "name": "load_skill",
      "source": "builtin"
    },
    {
      "approval": "never",
      "description": "Look up an order's current shipping status by its order id.",
      "executable": true,
      "idempotent": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "order_id": {
            "description": "The order id to look up, e.g. 'ORD-1001'.",
            "title": "Order Id",
            "type": "string"
          }
        },
        "required": [
          "order_id"
        ],
        "type": "object"
      },
      "name": "lookup_order",
      "source": "authored"
    },
    {
      "approval": "never",
      "description": "Retrieve the full content of a tool result that was spilled (replaced by a bounded preview and a notice naming a ref). Page through it with offsetBytes/limitBytes (raw byte slices of the stored text), or search it with a regex pattern to find where something is before paging to it.",
      "executable": false,
      "idempotent": true,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "limitBytes": {
            "description": "Maximum bytes to return, clamped to a hard ceiling of 16384. Default 16384.",
            "type": "integer"
          },
          "offsetBytes": {
            "description": "Byte offset into the spilled content to start reading from. Default 0.",
            "minimum": 0,
            "type": "integer"
          },
          "pattern": {
            "description": "A Python regular expression to search the full spilled content for, instead of paging. Returns up to 20 matches, each with its byte offset and a small context window, so a match's offset can be paged from with offsetBytes on a follow-up call.",
            "type": "string"
          },
          "ref": {
            "description": "The retrieval reference named in a spill notice (the originating tool call's id).",
            "type": "string"
          }
        },
        "required": [
          "ref"
        ],
        "type": "object"
      },
      "name": "read_tool_output",
      "source": "builtin"
    },
    {
      "approval": "never",
      "description": "Searches internal account notes for context relevant to a support ticket. Invoke with a short, specific natural-language request describing what to look for (an order id or account detail); it has no memory of the parent conversation.",
      "executable": true,
      "idempotent": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "message": {
            "description": "The message to delegate to this subagent.",
            "type": "string"
          }
        },
        "required": [
          "message"
        ],
        "type": "object"
      },
      "name": "researcher",
      "source": "subagent"
    },
    {
      "approval": "never",
      "description": "",
      "executable": false,
      "idempotent": false,
      "inputSchema": {
        "properties": {
          "b": {
            "title": "B",
            "type": "integer"
          }
        },
        "required": [
          "b"
        ],
        "title": "addArguments",
        "type": "object"
      },
      "name": "support_mcp__add",
      "source": "connection:crm/support_mcp"
    },
    {
      "approval": "always",
      "description": "Update one field on a customer record.",
      "executable": true,
      "idempotent": false,
      "inputSchema": {
        "additionalProperties": false,
        "properties": {
          "customer_id": {
            "description": "The customer id to update, e.g. 'CUST-42'.",
            "title": "Customer Id",
            "type": "string"
          },
          "field": {
            "description": "The customer field to change, e.g. 'email' or 'tier'.",
            "title": "Field",
            "type": "string"
          },
          "value": {
            "description": "The new value for that field.",
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "customer_id",
          "field",
          "value"
        ],
        "type": "object"
      },
      "name": "update_customer",
      "source": "bundle:crm"
    }
  ],
  "use": [
    "crm"
  ]
}
